git init
git add . or git add FILE> > adding to the stages
git commit -m "VERSION" >> adding to the repository

git status
git log

git diff FILE
git checkout FILE


git remote add origin LOCAL_GITHUB
git push -u origin master



git rm --cached -r "fileName"   >> to remove the fileNAME, or use . to remove everything